import React from 'react';

const TernaryBasics: React.FC = () => {
  return (
    <div>
      <p>The ternary operator is shorthand for if-else: <code>condition ? ifTrue : ifFalse</code></p>
      <p>Example: <code>const result = age {'>'}= 18 ? "Adult" : "Minor";</code></p>
    </div>
  );
};

export default TernaryBasics;